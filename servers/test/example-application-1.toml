#when copying change name, server, file_paths, parametres in pre_deploy command
[[stack]]
name = "test-zkouska"
[stack.config]
server = "test"
linked_repo = "int-gitops-1"
file_paths = [
    "apps/compose.yml",
    "apps/example-application-test/compose.yml",
]
pre_deploy.shell_mode = false
extra_args = ["--force-recreate"]